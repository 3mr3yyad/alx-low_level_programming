#include <stdio.h>
/**
 * main - start
 * Description: all possible combinations of two two-digit numbers
 * Return: 0 = success
*/
int main(void)
{
	int x = 0, y;
	while (x <= 99)
	{
		x = y;
		while (y <= 99)
		{
			if(x != z)
			{
			
			}
		y++;
		}
	x++;
	}
	putchar('\n');
	return (0);
}
